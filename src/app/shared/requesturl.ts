import { environment } from 'src/environments/environment'



export class RequestUrls{
    public static Urls={
        searchUrl : environment.baseUrl+'search/users',
        usersRep : environment.baseUrl+'users/'
     
    }

    
}